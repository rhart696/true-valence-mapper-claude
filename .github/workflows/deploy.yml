name: Deploy to GitHub Pages
# DISABLED 2026-02-22: v0 (vanilla HTML) is retired. v1 (Next.js) deploys via Vercel automatically.
# The GitHub Pages deployment is no longer needed. Re-enable only if v0 needs to be restored.

on:
  # push:  # disabled — was: branches: [main]
  workflow_dispatch:  # manual trigger still available for emergency use

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Validate HTML syntax
        run: |
          # Install HTML validator
          sudo apt-get update
          sudo apt-get install -y tidy

          # Validate HTML files (allow warnings, fail on errors only)
          for file in *.html; do
            if [ -f "$file" ]; then
              echo "Validating $file..."
              tidy -q -e "$file" || true
            fi
          done

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: '.'

  # Deployment job
  deploy:
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
